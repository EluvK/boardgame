[package]
name = "bg_runtime"
version = "0.1.0"
edition = "2021"

[dependencies]
boardgames_server = { path = "../boardgames_server" }
acquire_plugin = { path = "../acquire_plugin" }
planetx_plugin = { path = "../planetx_plugin" }
anyhow = "1.0"
tokio = { version = "1", features = ["full"] }
tracing = "0.1"
tracing-subscriber = "0.3"
serde = { version = "1.0", features = ["derive"] }
